spring:
  application:
    name: momentum
  datasource:
    url: jdbc:postgresql://localhost:5432/fitness_tracker
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
